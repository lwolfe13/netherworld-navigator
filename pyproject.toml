[project]
name = "netherworld-navigator"
version = "1.0.0"
description = "AI-powered legal aid chatbot for estate management teams"
dependencies = [
    "flask>=2.0.0",
    "flask-cors>=4.0.0"
]

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

